// jest-dom adds custom jest matchers for asserting on DOM nodes.
// This makes it easier to test DOM elements using more expressive assertions.

// For example, instead of using generic assertions like:
// expect(element).toBeInTheDocument()
// you can use specific ones like:
  // expect(element).toHaveTextContent(/react/i)

// This enhances the testing experience by providing a set of matchers 
// that are tailored for testing DOM nodes, which makes tests more readable and intuitive.

import '@testing-library/jest-dom';
