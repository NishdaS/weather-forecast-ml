# dependencies - These are automatically generated when you install your dependencies and should not be tracked.
# This includes Node.js modules and Yarn's Plug'n'Play (PnP) system.
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production - The build folder contains the compiled production-ready code (e.g., after running "npm run build" or "yarn build").
# It doesn't need to be version controlled because it's generated automatically from the source code.
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug log files generated during npm or yarn installation when an error occurs.
npm-debug.log*
yarn-debug.log*
yarn-error.log*
